<?xml version="1.0" encoding="utf-8"?>
<!--
  A password vault must never leave the device through Android's automatic
  backup or device-transfer paths. Both channels are excluded wholesale rather
  than by listing files, so a database added later is excluded by default.

  Sync happens only through the app's own end-to-end encrypted path.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
